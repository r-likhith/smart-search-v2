# dependencies
node_modules/

# environment — never commit real values ✅
.env

# Meilisearch data
data.ms/
dumps/

# logs — generated at runtime ✅
logs/
multiTenantLogs/

# OS
.DS_Store

# Excel/CSV exports
*.xlsx
*.csv

# old fix scripts (keep folder, ignore contents in git if needed)
# remove this line if you want oldFixFiles tracked ✅
# oldFixFiles/
oldFixFiles/

# runtime data — changes constantly ✅
learned/*.json
learned/*.backup.json

# keep directory structure ✅
!learned/.gitkeep

# sync state runtime ✅
sync_state/
data.ms/

# seed files — committed intentionally ✅
!learned/*.seed.json
backups/
config/apiKeys.json
config/apiKeys.json.tmp
benchmarks/*-before.json
benchmarks/*-after.json
benchmarks/2026-*.json
